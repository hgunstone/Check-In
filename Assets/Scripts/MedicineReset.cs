using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MedicineReset : MonoBehaviour
{/*

    public Transform medicine;

    public Vector3 pStartPos;
    public Vector3 bStartPos;
    public Vector3 gStartPos;

    public bool shouldReturn;

    public Object pinkMed;
    public Object blueMed;
    public Object greenMed;

    private void Start()
    {
        pStartPos = pinkMed.position;
        bStartPos = blueMed.position;

        gStartPos = greenMed.position;
    }

    void Update()
    {
        if(shouldReturn)
        {
            pinkMed.position = pStartPos;
            blueMed.position = bStartPos;
            greenMed.position = gStartPos;

            shouldReturn = false;
        }
    }*/
}
